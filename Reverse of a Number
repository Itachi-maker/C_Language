//Reverse of a Number
#include <stdio.h>
int main() {
int n, d, sum=0;
printf("Enter any number:");
scanf("%d", &n);
while(n){
    d=n%10;
    sum=sum*10+d;
    n=n/10;
}
printf("%d", sum);

    return 0;
}
